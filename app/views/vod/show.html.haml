= render partial: 'shared/breadcrumbs', locals: {page_name: recording.title}
#vod-item
  = render partial: 'vod/header'
  .container.content
    .row
      .col-md-12
        = render partial: 'shared/stream', locals: {file: "rtmp://streamer-01.codewatch.tv/vod/#{recording.name}", type: 'rtmp'}
