- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations'
  %h4 Forgot your Password ?
  %p
    Don't worry,
    %a.color-green{:href => new_password_path(resource_name)} click here
    to reset your password.
- if devise_mapping.confirmable? && controller_name != 'confirmations' && controller_name != 'passwords' && controller_name != 'registrations'
  = link_to 'Still waiting for confirmation instructions?', new_confirmation_path(resource_name)
- if devise_mapping.lockable? && resource_class.unlock_strategy_enabled?(:email) && controller_name != 'unlocks'
  = link_to "Didn't receive unlock instructions?", new_unlock_path(resource_name)
