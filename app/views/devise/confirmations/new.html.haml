= render partial: 'shared/breadcrumbs', locals: {page_name: 'Account Confirmation'}
.container.content
  .row
    .col-md-4.col-md-offset-4.col-sm-6.col-sm-offset-3
      = form_for(resource, as: resource_name, url: confirmation_path(resource_name), html: {class: 'reg-page'}) do |f|
        .reg-header
          %h2 Not getting the email?
        .input-group.margin-bottom-20
          %span.input-group-addon
            %i.fa.fa-user
          = f.email_field :email, autofocus: true, placeholder: 'Email address', required: '', type: 'text', class: 'form-control'
        .row
          .col-md-12.margin-bottom-10
            = f.submit 'Resend confirmation instructions', class: 'btn-u'
      %hr
        = render 'devise/shared/links'
